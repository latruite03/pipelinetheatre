// Maison de la création – MC Gare (Laeken)
// Repérage connector (stub): separate venue from MC NOH. Official site may be unreachable.

export async function loadMaisonDeLaCreationGare() {
  console.log('Maison de la création – MC Gare: stub (site currently unreachable). Returning 0 rows.')
  return []
}
