// Tour à Plomb (Bruxelles)
// NOTE: site has intermittent TLS/cert issues with server-side fetch in this environment.
// Keep as a stub for now; we can switch to scraping via browser/HTML snapshot or
// a secondary source (e.g., out.be) once stable.

export async function loadTourAPlomb() {
  console.log('Tour à Plomb: stub (site fetch unstable / agenda sometimes empty). Returning 0 rows.')
  return []
}
