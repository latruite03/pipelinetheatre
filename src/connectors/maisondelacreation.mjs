// Maison de la création (MC NOH)
// Repérage connector (stub): site may be temporarily unreachable (DNS/EAI_AGAIN observed).
// We keep the venue tracked; can switch to scraping via out.be or when the official site is back.

export async function loadMaisonDeLaCreation() {
  console.log('Maison de la création (MC NOH): stub (official site currently unreachable). Returning 0 rows.')
  return []
}
